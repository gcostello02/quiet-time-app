# Copy to .env and fill in your values. Used by docker compose build/up.
# Do not commit .env (it's in .gitignore).

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# Host port mapped to the container (optional; default 8081)
# PORT=8081
